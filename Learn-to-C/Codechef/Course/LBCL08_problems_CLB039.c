#include <stdio.h>
#include <string.h>
 
int main () {

    char x[] = "Hello";
    char y[] = "World";
    
    // declare string y with "World"
    
    // concatenate the two strings
    
    // print the final string
    strcat(x, y);
    printf("%s", x);

}